<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo con un color -->
    <item android:drawable="@drawable/rounded_shape" />

    <!-- Icono con color diferente -->
    <item
        android:drawable="@drawable/credit_card"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp"
        android:left="8dp"

        />
</layer-list>