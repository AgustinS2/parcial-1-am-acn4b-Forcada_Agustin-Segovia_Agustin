<?xml version="1.0" encoding="utf-8"?>
    <shape xmlns:android="http://schemas.android.com/apk/res/android">
        <solid android:color="@color/primaryColor" /> <!-- Color de fondo del botón -->
        <corners android:radius="22dp" />
        <stroke android:width="0.3dp" android:color="@color/black"/> <!-- Ancho y color del borde -->
    </shape>
